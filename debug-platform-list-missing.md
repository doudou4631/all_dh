# [OPEN] platform-list-missing

## 背景
- 用户反馈：部署后“平台范围”显示结果不对，与预期不一致。
- 当前已完成一次全量发布，但实际页面结果仍需通过运行时证据确认。

## 复现目标
- 确认后台“标记模板 -> 修改标记模板”里的“平台范围”候选是否完整。
- 确认服务端 `/server/markTemplate/platformOptions` 实际返回了哪些平台。

## 初始假设
- 假设 1：后端接口实际返回的平台数量仍不完整，问题不在前端渲染。
- 假设 2：接口返回了平台，但受当前登录用户模板/权限范围限制，导致看起来“不全”。
- 假设 3：菜单平台码与模板历史平台码存在编码映射差异，导致展示与预期不一致。
- 假设 4：浏览器仍命中旧静态资源，实际页面未使用到刚部署的前端产物。
- 假设 5：用户看到的“不对”不是数量问题，而是平台名称/编码映射错误。

## 证据计划
- 先采集服务器运行状态、静态资源 hash、接口真实返回。
- 若接口需要登录态，再补采登录后请求证据。
- 基于证据决定是否需要继续修代码或修数据库。
